import 'package:flutter/material.dart';

class PlaygroundView extends StatelessWidget {
  const PlaygroundView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('This is the PlaygroundView'),
    );
  }
}
